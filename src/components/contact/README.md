## /contact
- **`Contact.jsx`** - Contact form with loading, success, and error states. Accepts an optional `source` prop that defaults to `"home"`
- **`handleSend.js`** - Cleans and validates email/message input, then submits it with the source to the `handle-message` Edge Function
