### src/hooks
- **`useIntersectionObserver.js`** - Tracks current and previous viewport intersection for lazy loading and visibility-gated rendering or animation
- **`useNavigateToSection.js`** - Smooth-scrolls to a Home section in place, or routes to Home with the requested section in navigation state