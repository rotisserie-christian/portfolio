# Hooks

## Shared Hooks
- **`useIntersectionObserver.js`** - Observes element intersection with viewport for lazy loading

## Shared Hooks (expanded)
1. **useIntersectionObserver** (DemoSequencer, Home)
   - Observes when element enters viewport, for progressive loading
   - Returns `hasIntersected: boolean`
   - Used for lazy loading heavy components (sequencer, flowchart)
